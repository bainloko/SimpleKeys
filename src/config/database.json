{
    "development": {
        "dialect": "sqlite",
        "dialectModule": "require('@journeyapps/sqlcipher')",
        "logging": "msg => log.info(msg)",
        "storage": "../database.db"
    },
    "test": {
        "dialect": "sqlite",
        "dialectModule": "require('@journeyapps/sqlcipher')",
        "logging": "msg => log.info(msg)",
        "storage": ":memory:"
    },
    "production": {
        "dialect": "sqlite",
        "dialectModule": "require('@journeyapps/sqlcipher')",
        "logging": "msg => log.info(msg)",
        "storage": ""
    },
    "define": {
        "timestamps": true
    }
}