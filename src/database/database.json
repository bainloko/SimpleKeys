{
    "development": {
        "dialect": "sqlite",
        "dialectModulePath": "better-sqlite3-multiple-ciphers",
        "logging": "msg => log.info(msg)",
        "storage": "../database.db"
    },
    "test": {
        "dialect": "sqlite",
        "dialectModulePath": "better-sqlite3-multiple-ciphers",
        "logging": "msg => log.info(msg)",
        "storage": ":memory"
    },
    "production": {
        "dialect": "sqlite",
        "dialectModulePath": "better-sqlite3-multiple-ciphers",
        "logging": "msg => log.info(msg)"
    }
}