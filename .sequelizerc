//.sequelizerc configurações

import { resolve } from "path";

export default {
    "config": resolve("src", "config", "database.json"),
    "models-path": resolve("src", "models"),
    "seeders-path": resolve("src", "models", "seeders"),
};